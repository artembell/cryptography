const encipher = ({text, key}) => {
    console.log('vigenere encipher')
}

const decipher = ({text, key}) => {
    console.log('vigenere decipher')
}

export const vigenere = {
    encipher,
    decipher
}